class Expense {
  constructor(id, category, amount, description) {
    this.id = id;
    this.category = category;
    this.amount = amount;
    this.description = description;
  }
}

export default Expense;
